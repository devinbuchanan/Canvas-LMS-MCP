{
  "name": "canvas-lms-mcp",
  "version": "0.1.0",
  "description": "HTTP-based Model Context Protocol server for Canvas LMS integration.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p .",
    "start": "node dist/index.js",
    "test": "rm -rf dist && tsc -p . && node --test dist/__tests__"
  },
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "canvas",
    "mcp",
    "json-rpc",
    "server"
  ],
  "author": "",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.6.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.9.3"
  }
}
